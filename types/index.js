export {}
/* typal types/settings.xml closure noSuppress */
/**
 * @typedef {_expensive.Settings} Settings `＠record` Settings saved in ~/.namecheaprc file.
 */
/**
 * @typedef {Object} _expensive.Settings `＠record` Settings saved in ~/.namecheaprc file.
 * @prop {string} ApiUser Namecheap user.
 * @prop {string} ApiKey Namecheap API key.
 * @prop {string} ClientIp Client IP.
 * @prop {string} phone Last 3 digits of the phone number.
 */
